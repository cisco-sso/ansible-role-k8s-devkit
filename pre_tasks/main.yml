---
# pre_tasks file for ansible-role-k8s-devkit

- name: kdk | Include defaults as vars to allow default config of role dependencies.
  include_vars:
    dir: '../defaults'
    files_matching: 'main.yml'
